# Parser fixtures

Static HTML fixtures are intentionally minimal and synthetic. They represent stable markup contracts (currently JSON-LD Product/Offer) and must not contain personal data, cookies, session tokens, or live credentials.

Use fixtures for deterministic adapter tests. Live marketplace requests belong to integration tests and must respect robots.txt, rate limits, terms, and marketplace access controls.
